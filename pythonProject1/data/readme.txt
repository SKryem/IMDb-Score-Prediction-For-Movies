filtered_movie_metadata.csv contains the original dataset with the following changes:
movie_title duplicates were dropped

filtered_merged_movie_metadata is the same as filtered_movie_metadata.csv  with the following changes:
our new features were added
rows(samples) with no movie reviews were removed